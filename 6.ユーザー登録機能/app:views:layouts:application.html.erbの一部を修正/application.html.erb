<%# ヘッダー内を以下に書き換える %>
<header class="header">
  <div class="header__bar row">
    <h1 class="grid-6"><a href="/">OCATweet</a></h1>
    <% if ユーザーがログインしているのかで条件分岐を行う（deviseのメソッドを使いましょう） %>
      <div class="user_nav grid-6">
        <span><%= ログイン中のユーザー名を表示させる %>
        <ul class="user__info">
          <li>
            <%= link_to "マイページ", root_path %>
            <%= link_toメソッドを使って、ログアウト画面へ遷移させましょう（表示させる文字は「ログアウト」）, class: 'post' %>
          </li>
        </ul>
        </span>
        <%= link_to  "投稿する", new_tweet_path, class: "post" %>
      </div>
    <% else %>
      <div class="grid-6">
        <%= link_to link_toメソッドを使って、ログイン画面へ遷移させましょう（表示させる文字は「ログイン」）, class: 'post' %>
        <%= link_to link_toメソッドを使って、新規登録画面へ遷移させましょう（表示させる文字は「新規登録」）, class: 'post' %>
      </div>
    <% end %>
  </div>
</header>
